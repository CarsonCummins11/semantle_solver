This is a simple script to beat the semantle game @ semantle.com.

First, ensure all requirements are installed (listed in requirements.txt)

Next, run the app with `python3 app.py` , and follow the instructions in terminal.

Generally, it gets it in 2-4 guesses.
